/**/
@-ms-viewport{width:device-width;}
@-o-viewport{width: device-width;}
@viewport{width: device-width;}
/**/
*:not(head):not(script):not(title):not(style),
:before,
:after{
  display:inline-flex;
  box-sizing:border-box;
  margin: 0;
  padding: 0;
  border:none;
  outline:none;
}
/**/
:root{

}
body,html{
  display:block !important;
}
html{
  // width: 100vw;
  // height:100vh;
  // font-size: ;
  // max-width: $largest-col-width;
  // font-size: 100vw;
  // font-size: calc(#{$largest-col-width} + 100vw);
  // font-size:calc(100vw);
  // font-size: calc(
  // 100vw -
  //   (
  //     (-100vw - #{$largest-col-width})
  //     *
  //     (100vw - #{$largest-col-width})
  //   )
  // );
}
  body{
    // @include font-size-line-height($main-font-size, $main-line-height, $view-min-font-size);
    // font-family:$view-main-font;
    // color:$view-main-color;
    overflow:auto;
    overflow-x:hidden;
    width: 100%;
      *{
        font-size:inherit;
      }
  }

b,h1,h2,h3,h4,h5,h6,strong{
  font-weight:normal
}
html
,svg:not(:root){
  // overflow:hidden;
}
svg{
  max-width: 100%;
  max-height: 100%;
}

button,fieldset,input{

}
body,fieldset,figure,h1,h2,h3,h4,h5,h6,html,p,ul{

}
img,legend{

}
body,button,fieldset,html,legend,p,td,th,ul{

}

/*article,aside,details,figcaption,figure,footer,header,hgroup,main,menu,nav,section,summary{display:block}*/
/*audio,canvas,progress,video{display:inline-block;vertical-align:baseline}*/
audio:not([controls]){
  display:none;
  height:0;
}
[hidden],template{
  display:none;
}
a{
  background-color:transparent;color:inherit;text-decoration:none;cursor:pointer
}
a:active,a:hover{

}
abbr[title]{

}
dfn{

}
mark{
  background:inherit;
}
small{
  font-size:inherit;
}
sub,sup{
  font-size:inherit;
  line-height:inherit;
  vertical-align:baseline;
  position:relative;
}
sup{
  top:0;
}
sub{
  bottom:0;
}
img{
  // max-width:100%;
}
hr{
  // box-sizing:content-box;
  // height:0;
}
pre,textarea{
  overflow:auto;
}
code,kbd,pre,samp{
  // font-family:monospace,monospace;
  font-size:inherit;
}
button,input,optgroup,select,textarea{
  color:inherit;
  font:inherit;
  background: transparent;
}
address,i{
  font-style:normal;
}
button{
  // overflow:visible;
}
button
,select{
  text-transform:none;
}
button
,[type=button]
,[type=reset]
,[type=submit]{
  -webkit-appearance:button;
  cursor:pointer;
}
button[disabled]
,html input[disabled]{
  cursor:no-drop;
}
input[disabled]{
  // background:#f2eeed;
}
button::-moz-focus-inner
,input::-moz-focus-inner{

}
input{
  line-height:normal;
}
input[type=checkbox]
,input[type=radio]{
  // box-sizing:border-box;
  // width:auto;
}
input[type=number]::-webkit-inner-spin-button
,input[type=number]::-webkit-outer-spin-button{
  height:auto
}
input[type=search]{
  -webkit-appearance:textfield;
}
input[type=search]::-webkit-search-cancel-button
,input[type=search]::-webkit-search-decoration{
  -webkit-appearance:none
}
optgroup{
  font-weight:normal;
}
table{
  border-collapse:collapse;
  border-spacing:0;
}
li{
  list-style:none;
}
map{
  // display:inline-block;
}
::-webkit-input-placeholder{
  font-size:inherit;
  line-height:inherit;
  color:inherit;
}
::-moz-placeholder{
  font-size:inherit;
  line-height:inherit;
  color:inherit;
}
:-ms-input-placeholder{
  font-size:inherit;
  line-height:inherit;
  color:inherit;
}
::placeholder{
  font-size:inherit;
  line-height:inherit;
  color:inherit;
}
::-moz-selection{
  background:/*rgba($color-group1__5,1)*/#00e0e0;
}
::selection{
  background:/*rgba($color-group1__5,1)*/#00e0e0;
}
